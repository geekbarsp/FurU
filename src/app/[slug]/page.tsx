import { notFound } from "next/navigation";import { routeLabels } from "@/lib/data";import SupportingClient from "@/components/SupportingClient";
export default async function SupportingPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=routeLabels[slug];if(!title)notFound();const descriptions:Record<string,string>={resources:"Practical, vet-informed guides for every stage of life together.",volunteer:"Give time, transport, or professional skills to verified welfare groups.",donations:"Direct support toward transparent, verified animal welfare work.",messages:"Keep conversations, files, and plans together without sharing private contact details.",appointments:"Plan interviews, visits, meet-and-greets, pickups, and wellness checks.",verification:"Small trust-building steps create safer matches for everyone.","lost-and-found":"Help bring a missing companion home without exposing private addresses.",foster:"Offer temporary safety when a pet needs somewhere to land.",help:"Straight answers and caring support for every FurU journey.",privacy:"How FurU protects people, animals, and sensitive information.",terms:"The shared standards that keep FurU responsible and kind.",admin:"Moderation, verification, reports, and welfare oversight."};return <main className="page supporting-page"><div className="shell"><div className="page-title"><span className="eyebrow">FurU community</span><h1>{title}</h1><p>{descriptions[slug]}</p></div><SupportingClient slug={slug}/></div></main>}
